# Copy to .env.local — base URL for admin_of_admins_be (no trailing slash)
VITE_ADMIN_API_BASE_URL=http://localhost:9302

# Prod/preprod admin backend base URL (no trailing slash)
VITE_ADMIN_PROD_API_BASE_URL=https://beadmin.gogamify.xyz

# WhatsApp integration backend (WAHA routing server) — separate from admin API
# Local dev WhatsApp backend (no trailing slash)
VITE_WHATSAPP_API_LOCAL_BASE_URL=http://localhost:9080

# Prod/preprod WhatsApp backend (no trailing slash)
VITE_WHATSAPP_API_BASE_URL=https://whatsapp.mainstreetcpltd.com
